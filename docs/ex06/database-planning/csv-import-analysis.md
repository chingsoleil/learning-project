# CSV 資料匯入評估報告

## 📊 資料概況

### 檔案資訊
- **英文題庫**: `ex05/IPIP_items.csv` (3,805 題)
- **中文題庫**: `ex05/IPIP_items-zh-tw.csv` (3,805 題)
- **資料狀態**: ✅ 兩份檔案筆數一致，一對一對應

### CSV 欄位結構
```csv
instrument,alpha,key,text,label
```

| 欄位 | 說明 | 範例 |
|------|------|------|
| instrument | 量表名稱 | `16PF`, `NEO`, `VIA` |
| alpha | 信度係數 (Cronbach's α) | `0.78` |
| key | 計分方向 | `1` (正向), `-1` (反向) |
| text | 題目文字 | `Act wild and crazy.` |
| label | 特質面向 | `Gregariousness`, `Anxiety` |

---

## 📈 資料統計

### 1. 量表（Instrument）分布
- **總量表數**: 36 種
- **總題數**: 3,805 題

| 量表名稱 | 題數 | 佔比 |
|---------|------|------|
| AB5C | 456 | 12.0% |
| CPI | 313 | 8.2% |
| VIA | 252 | 6.6% |
| 6FPQ | 229 | 6.0% |
| TCI | 228 | 6.0% |
| HEXACO_PI | 223 | 5.9% |
| CAT-PD | 210 | 5.5% |
| ORAIS | 200 | 5.3% |
| HPI-HIC | 198 | 5.2% |
| NEO | 185 | 4.9% |
| 16PF | 172 | 4.5% |
| BFAS | 125 | 3.3% |
| JPI | 109 | 2.9% |
| MPQ | 95 | 2.5% |
| 其他 22 種 | 610 | 16.2% |

### 2. 特質面向（Label）分布
- **總特質數**: 246 種
- **高頻特質** (Top 10):

| 特質名稱 | 題數 |
|---------|------|
| Creativity/Originality | 56 |
| Intellect | 50 |
| Leadership | 48 |
| Conscientiousness | 47 |
| Gregariousness | 47 |
| Risk-taking/Sensation-Seeking | 47 |
| Agreeableness | 46 |
| Assertiveness | 46 |
| Dutifulness | 45 |
| Sociability | 45 |

### 3. 計分方向（Key）分布
- **正向題 (key=1)**: 約 70%
- **反向題 (key=-1)**: 約 30%

### 4. 信度係數（Alpha）範圍
- **範圍**: 0.73 ~ 0.86
- **平均**: 約 0.80（良好信度）

---

## 🔄 與資料字典的對應關係

### 您的資料字典結構：

```
1. InstrumentCategory（量表分類主檔）
   ├─ Id, NameEn, NameZh, Code, Description

2. TraitCategory（特質面向分類主檔）
   ├─ Id, NameEn, NameZh, Code, Description

3. QuestionBank（題庫主檔）
   ├─ Id, InstrumentCategoryId, TraitCategoryId
   ├─ TextEn, TextZh, Alpha, Key, QuestionCode
```

### 對應關係矩陣：

| CSV 欄位 | 資料字典對應表 | 資料字典欄位 | 對應狀態 |
|---------|--------------|------------|---------|
| instrument | InstrumentCategory | NameEn | ✅ 需建立對照表 |
| instrument | InstrumentCategory | NameZh | ⚠️ 需補充翻譯 |
| label | TraitCategory | NameEn | ✅ 需建立對照表 |
| label | TraitCategory | NameZh | ⚠️ 需補充翻譯 |
| text (EN) | QuestionBank | TextEn | ✅ 直接對應 |
| text (ZH) | QuestionBank | TextZh | ✅ 直接對應 |
| alpha | QuestionBank | Alpha | ✅ 直接對應 |
| key | QuestionBank | Key | ✅ 直接對應 |

---

## ⚠️ 資料結構問題與建議

### 問題 1：重複題目
**現象**: 
- 同一題目可能屬於多個 label
- 例：`Am not afraid of providing criticism.` 同時屬於 `Dominance` 和 `Assertiveness`

**影響**:
- CSV 有 3,805 列，但**實際不重複題目可能少於此數**
- 會產生重複的題目記錄

**解決方案**:
```
選項 A【推薦】: 保留重複，一題多標籤
├─ QuestionBank 建立 3,805 筆記錄
├─ 允許同一題目文字對應不同特質
└─ 優點：保留原始資料完整性，符合心理測驗設計

選項 B: 去重，一題一記錄 + 多對多關聯表
├─ QuestionBank 只建立不重複題目（約 3,200 筆）
├─ 新增 QuestionBank_TraitCategory 關聯表
└─ 優點：節省空間，但需要額外開發
```

### 問題 2：缺少中文翻譯
**現象**:
- `instrument` 只有英文代碼（如 `16PF`）
- `label` 只有英文名稱（如 `Gregariousness`）

**影響**:
- InstrumentCategory.NameZh 缺少翻譯
- TraitCategory.NameZh 缺少翻譯

**解決方案**:
```
方案 1【推薦】: 手動建立翻譯對照表
├─ 建立 instrument_translations.csv (36 筆)
└─ 建立 label_translations.csv (246 筆)

方案 2: 自動翻譯 + 人工校對
├─ 使用 AI 翻譯工具批次翻譯
└─ 人工審核心理學專業術語

方案 3: 暫時使用英文
├─ NameZh 填入與 NameEn 相同內容
└─ 未來逐步補充翻譯
```

### 問題 3：缺少 Code 欄位
**現象**:
- CSV 沒有提供 `Code` 欄位
- 資料字典設計了 `InstrumentCategory.Code` 和 `TraitCategory.Code`

**解決方案**:
```sql
-- 自動生成 Code
InstrumentCategory.Code = instrument 原值（如 '16PF'）
TraitCategory.Code = 自動編號（如 'TRAIT_001'）或英文縮寫

-- 範例：
16PF → Code: '16PF'
Gregariousness → Code: 'GREG' 或 'TRAIT_001'
```

### 問題 4：缺少 QuestionCode
**現象**:
- CSV 沒有唯一題目編號
- 資料字典設計了 `QuestionBank.QuestionCode`

**解決方案**:
```sql
-- 生成規則：{Instrument}_{Label縮寫}_{流水號}
範例：
16PF_GREG_001
16PF_ANX_001
NEO_OPEN_001

-- SQL 範例：
CONCAT(instrument, '_', 
       SUBSTRING(label, 1, 4), '_', 
       LPAD(ROW_NUMBER, 3, '0'))
```

---

## ✅ 匯入可行性評估

### 結論：**✅ 可以匯入，但需調整**

### 必要調整項目：

| 調整項目 | 優先級 | 工作量 | 說明 |
|---------|-------|--------|------|
| 建立 instrument 翻譯表 | 🔴 高 | 2 小時 | 36 筆，需心理學背景 |
| 建立 label 翻譯表 | 🔴 高 | 8 小時 | 246 筆，需專業審核 |
| 生成 QuestionCode | 🟡 中 | 1 小時 | 程式自動生成 |
| 處理重複題目 | 🟡 中 | 2 小時 | 決定保留或去重 |
| 生成 TraitCategory.Code | 🟢 低 | 1 小時 | 程式自動生成 |

---

## 📝 建議的匯入流程

### 階段 1：準備階段
```
1. 建立 instrument 中文對照表
   - 輸入：36 個 instrument 名稱
   - 輸出：instrument_zh.csv

2. 建立 label 中文對照表
   - 輸入：246 個 label 名稱
   - 輸出：label_zh.csv

3. 決定重複題目處理策略
   - 建議：保留重複（選項 A）
```

### 階段 2：匯入 InstrumentCategory
```sql
INSERT INTO InstrumentCategory (NameEn, NameZh, Code)
SELECT DISTINCT 
    instrument AS NameEn,
    -- 從對照表取得中文
    (SELECT zh_name FROM instrument_zh WHERE en_name = instrument) AS NameZh,
    instrument AS Code
FROM IPIP_items;
```

### 階段 3：匯入 TraitCategory
```sql
INSERT INTO TraitCategory (NameEn, NameZh, Code)
SELECT DISTINCT 
    label AS NameEn,
    -- 從對照表取得中文
    (SELECT zh_name FROM label_zh WHERE en_name = label) AS NameZh,
    CONCAT('TRAIT_', LPAD(ROW_NUMBER(), 3, '0')) AS Code
FROM IPIP_items;
```

### 階段 4：匯入 QuestionBank
```sql
INSERT INTO QuestionBank (
    InstrumentCategoryId, TraitCategoryId, 
    TextEn, TextZh, Alpha, Key, QuestionCode
)
SELECT 
    ic.Id,
    tc.Id,
    en.text,
    zh.text,
    en.alpha,
    en.key,
    CONCAT(en.instrument, '_', 
           SUBSTRING(en.label, 1, 4), '_', 
           LPAD(ROW_NUMBER(), 4, '0'))
FROM IPIP_items_en en
INNER JOIN IPIP_items_zh zh ON en.row_num = zh.row_num
INNER JOIN InstrumentCategory ic ON en.instrument = ic.NameEn
INNER JOIN TraitCategory tc ON en.label = tc.NameEn;
```

---

## 🎯 立即行動建議

### 選項 1：快速啟動（2-3 天）
```
✅ 優先完成技術實作
1. 使用英文欄位暫代中文（NameZh = NameEn）
2. 自動生成所有 Code
3. 保留重複題目
4. 完成資料匯入測試
5. 後續逐步補充翻譯
```

### 選項 2：完整準備（1-2 週）
```
✅ 優先完成翻譯品質
1. 先完成所有翻譯對照表
2. 人工審核專業術語
3. 處理重複題目邏輯
4. 完整測試後匯入
```

---

## 📂 需要產出的檔案

為了順利匯入，建議產出以下檔案：

1. **instrument_translations.csv**
   ```csv
   instrument_en,instrument_zh,description
   16PF,16種人格因子,卡特爾16種人格因子問卷
   NEO,NEO人格量表,五大人格特質量表
   ...
   ```

2. **label_translations.csv**
   ```csv
   label_en,label_zh,description
   Gregariousness,合群性,喜歡與他人互動的傾向
   Anxiety,焦慮,容易擔心和緊張的傾向
   ...
   ```

3. **import_script.sql**
   - 完整的資料匯入 SQL 腳本
   - 包含所有轉換邏輯

---

## 🤔 需要您決定的事項

請告訴我您的選擇：

1. **重複題目處理**：
   - [ ] A. 保留重複，一題多標籤（推薦）
   - [ ] B. 去重，建立多對多關聯表

2. **翻譯策略**：
   - [ ] A. 快速啟動，先用英文
   - [ ] B. 完整準備，先完成翻譯

3. **匯入範圍**：
   - [ ] A. 全部匯入 (3,805 題)
   - [ ] B. 先匯入部分量表測試（如只匯入 16PF 的 172 題）

4. **是否需要我協助**：
   - [ ] 產生翻譯對照表範本
   - [ ] 撰寫匯入 SQL 腳本
   - [ ] 建立資料驗證腳本

---

## 📌 總結

✅ **可以匯入**：CSV 結構與資料字典基本相容  
⚠️ **需調整**：補充翻譯、生成 Code、處理重複  
🎯 **建議**：採用「快速啟動」策略，先完成技術實作，後續補充翻譯

**預估工時**：
- 快速版：2-3 天（含測試）
- 完整版：1-2 週（含翻譯審核）

