# CSV 準備說明

## 📋 檔案位置

所有 CSV 檔案都應該放在這個資料夾（`ex06/`）中。

---

## ✅ 需要準備的 CSV 檔案

### 1. `IPIP_items-merged.csv`（主要匯入檔案）

**狀態**：目前只有標題列，需要填入 3,805 題的完整資料

**欄位結構**（7 個欄位）：
```csv
instrument,alpha,key,text_en,text_zh,label,IPIP_item_number
```

**資料來源**：
- 使用 Excel 檔案 `DataMerge.xlsx` 整理
- 合併三個原始 CSV：
  - `IPIP_items.csv` → 取得 `instrument`, `alpha`, `key`, `text_en`, `label`
  - `IPIP_items-zh-tw.csv` → 取得 `text_zh`（對應同一列）
  - `IPIP3320.csv` → 取得 `IPIP_item_number`（透過題目文字匹配）

**重要**：
- ✅ 必須是 **UTF-8 without BOM 編碼**（不要 BOM）
- ✅ 必須包含標題列（第一列）
- ✅ 總共 3,805 列資料（不含標題列）

---

### 2. `instrument_translations_template.csv`（翻譯範本）

**狀態**：✅ 已就位（36 個量表）

**欄位結構**：
```csv
instrument_en,instrument_zh,description
```

**說明**：
- `instrument_en`：已填入 36 個不重複的量表名稱
- `instrument_zh`：可選，填入中文翻譯
- `description`：可選，填入描述

---

### 3. `label_translations_template.csv`（翻譯範本）

**狀態**：✅ 已就位（246 個特質）

**欄位結構**：
```csv
label_en,label_zh,description
```

**說明**：
- `label_en`：已填入 246 個不重複的特質名稱
- `label_zh`：可選，填入中文翻譯
- `description`：可選，填入描述

---

## ⚠️ 關鍵注意事項

### UTF-8 編碼（重要：不要 BOM）

**所有 CSV 檔案都必須是 UTF-8 without BOM 編碼！**

⚠️ **關鍵**：使用 **UTF-8 without BOM**，不是 UTF-8 with BOM。

#### Excel 儲存為 UTF-8 without BOM：

**推薦方法**：
1. Excel 另存為一般 CSV（CSV (逗號分隔) (*.csv)）
2. 用記事本開啟
3. **檔案** → **另存新檔**
4. **編碼**選擇：**UTF-8**（⚠️ 不是「UTF-8 with BOM」）
5. 儲存

#### 驗證方法：

- 用記事本開啟 CSV
- **檔案** → **另存新檔**
- 檢查編碼欄位顯示為「UTF-8」（不是「UTF-8 with BOM」）

**詳細說明**：請參考 `docs/ex06/database-planning/UTF8_BOM_GUIDE.md`

---

## 📂 檔案清單

### 原始資料檔案（參考用，不需要匯入）

- `IPIP_items.csv` - 英文題庫（3,807 列）
- `IPIP_items-zh-tw.csv` - 中文題庫（3,807 列）
- `IPIP3320.csv` - ItemNumber 參考（3,325 列）

### 工作檔案

- `DataMerge.xlsx` - Excel 合併工具

### 最終匯入檔案

- ✅ `IPIP_items-merged.csv` - **需要準備**（3,805 題）
- ✅ `instrument_translations_template.csv` - 已就位
- ✅ `label_translations_template.csv` - 已就位

---

## ✅ 完成後檢查

準備完成後，確認：

- [ ] `IPIP_items-merged.csv` 已準備完成（3,805 題，UTF-8 編碼）
- [ ] `instrument_translations_template.csv` 已就位（36 個量表）
- [ ] `label_translations_template.csv` 已就位（246 個特質）
- [ ] 所有 CSV 檔案都是 **UTF-8 without BOM 編碼**
- [ ] 所有 CSV 檔案都有正確的標題列

---

## 🎯 下一步

準備完成後，執行資料匯入：

1. 執行 `docs/ex06/database-planning/CREATE_DATABASE.sql` 建立資料庫結構
2. 執行 `docs/ex06/database-planning/INSERT_DATA_DIRECT.sql` 匯入資料

詳細說明請參考：`docs/ex06/database-planning/README.md`

---

**最後更新**：2024-12-07

